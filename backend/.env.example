# Banco de Dados MySQL
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=SUA_SENHA_AQUI
DB_NAME=bncc_admin

# JWT — gere um secret forte em produção
JWT_SECRET=MUDE_PARA_UM_SECRET_FORTE_EM_PRODUCAO
JWT_EXPIRES_IN=8h

# Servidor
PORT=3001
NODE_ENV=development

# CORS — origem permitida (frontend)
FRONTEND_URL=http://localhost:5173
